/** How a search query is matched against the catalogue. */
export type SearchType = "name" | "ingredient";

/** Alcohol filter applied on top of a search. */
export type AlcoholicFilter = "all" | "alcoholic" | "non-alcoholic";
